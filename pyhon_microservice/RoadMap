Реализовать HighLoad Service при помощи следующих инструментов:

- Reddis (Кеширование запросов)
- Kubernetes (Load balancer + Service live controller)
- Postman (Request generator)
- Postgres (Database)
- Fastapi (инстурмент для создания эндпоинтов)
- Docker (deploy handler)

1) Использовать import asyncpg и from contextlib import asynccontextmanager
2) Обработать ответы postgre api